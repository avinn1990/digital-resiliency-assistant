# UI

React + Vite front end for the Digital Resiliency Assistant.

## Development

```bash
npm install
npm run dev
```

Set `VITE_API_URL` to the backend (e.g. `http://localhost:8000`). In dev, `/api` is proxied to the backend when `VITE_API_URL` is unset.
